# Java Microbenchmark Harness (JMH) tests

## Usage

mvn clean install
java -jar target/benchmarks.jar >results.log

in folders jmh_test (StringConcatBenchmark) 
and jmh_test2 (SortingBenchmark)

and wait end 
see results.log
